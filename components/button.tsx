import { Text, View, StyleSheet, Pressable } from "react-native";

type Props = { 
    label : string;
};

export default function Button ({label} : Props) {
    return (
        <view>
            <Pressable>
                
            </Pressable>

        </view>
    );
}